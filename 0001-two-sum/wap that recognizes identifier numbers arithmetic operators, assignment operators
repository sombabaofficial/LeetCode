import ply.lex as lex

tokens = ('ID', 'NUMBER', 'PLUS', 'MINUS', 'MUL', 'DIV', 'ASSIGN')

t_PLUS   = r'\+'
t_MINUS  = r'-'
t_MUL    = r'\*'
t_DIV    = r'/'
t_ASSIGN = r'='

def t_ID(t):
    r'[a-zA-Z_][a-zA-Z0-9_]*'
    return t

def t_NUMBER(t):
    r'\d+'
    t.value = int(t.value)
    return t

t_ignore = ' \t'

def t_error(t):
    print("Invalid:", t.value[0])
    t.lexer.skip(1)

lexer = lex.lex()

data = input("Enter input: ")
lexer.input(data)

for tok in lexer:
    print(tok)
